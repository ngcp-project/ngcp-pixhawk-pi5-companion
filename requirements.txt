# ============================================================
# NGCP Pixhawk Pi 5 Companion – Python Dependencies
# ============================================================
#
# IMPORTANT: This project uses the MAVProxy pipx-managed venv.
# Do NOT install these packages into the system Python.
#
# Installation Instructions:
#   1. Install MAVProxy via pipx (this also installs pymavlink):
#      pipx install mavproxy
#
#   2. Inject any additional packages into that same venv:
#      pipx inject mavproxy <package-name>
#
# External Repository Dependency (NOT pip-installable):
#   The GCS translation pipeline depends on the gcs-infrastructure
#   repository being cloned to: /home/ngcp25/gcs-infrastructure
#
#   Clone command:
#     git clone https://github.com/ngcp-project/gcs-infrastructure.git /home/ngcp25/gcs-infrastructure
#
#   Modules used (InfrastructureInterface API, gcs-infrastructure >= 2026-03):
#     - InfrastructureInterface (LaunchVehicleXBee, SendTelemetry, ReceiveCommand)
#     - Telemetry.Telemetry
#
# ============================================================

# Core MAVLink proxy and Python bindings
# (pymavlink is installed automatically as a MAVProxy dependency)
MAVProxy>=1.8.60
pymavlink>=2.4.40
